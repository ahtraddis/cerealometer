import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
	device: {
		marginBottom: 10,
	},
	name: {
		alignItems: "center",
		backgroundColor: "darkgreen",
		padding: 10,
	},
	text: {
		color: "#fff",
	}
})
