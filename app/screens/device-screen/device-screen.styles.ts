import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
	button: {
		width: 120,
		backgroundColor: "#5D2555",
		padding: 5,
		marginTop: 10,
		marginBottom: 10,
	},
	buttonText: {
		fontSize: 14,
	},
});